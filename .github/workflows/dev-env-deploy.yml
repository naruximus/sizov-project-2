name: Deploy Straou to DevServer

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Build and Deploy to Server
        env:
          SERVER_USER: ${{ secrets.DEV_SERVER_USER }}
          SERVER_PASSWORD: ${{ secrets.DEV_SERVER_PASSWORD }}
          SERVER_IP: ${{ secrets.DEV_SERVER_IP }}
        run: |
          sshpass -p "$SERVER_PASSWORD" ssh $SERVER_USER@$SERVER_IP "cd sizov-project-2 && pnpm install"
          sshpass -p "$SERVER_PASSWORD" ssh $SERVER_USER@$SERVER_IP "cd sizov-project-2 && pnpm run --filter strapi build"
          sshpass -p "$SERVER_PASSWORD" ssh $SERVER_USER@$SERVER_IP "cd sizov-project-2 && forever start -c "pnpm run --filter strapi start" ./"
